#include "HARTProtocolDeclarations.h"
#include "HARTProtocolErrors.h"
#include "HARTProtocolDLL.h"

/*
extern HART_Protocol_Errors HARTProtocolErrors;

HARTProtocolDLL hart(PL_CHANNEL_STDIO);
HARTCommand ccommand;
char buffer[256];
unsigned char bsize;
*/

void setup()
{
	/*
	HARTCommand ccommand = COMMAND0;
	buffer[0] = 0;
	bsize = 0;
	*/
}

void loop()
{
	/*
	hart.TRANSMITindicate(ccommand, buffer, bsize);
	hart.TRANSMITresponse(ccommand, buffer, bsize);
	*/
}
